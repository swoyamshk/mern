const numbers = [10,20,30,40,50];

// const foundNumber = numbers.find((num) => num>30); //num acts as a for loop;
// console.log(foundNumber);

const filteredNumber = number.filter((num)=>num>30);
console.log(filteredNumber);

const squaredNumber = number.map(num =>num*num);
console.log(squaredNumber);
